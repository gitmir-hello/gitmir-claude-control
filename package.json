{
  "name": "gitmir-claude-control",
  "version": "1.0.0",
  "description": "Local dashboard for running Claude Code across projects: models each product from the real code, turns any part of that model into an exact task, and verifies the work before calling it done.",
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "homepage": "https://github.com/gitmir-hello/gitmir-claude-control",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gitmir-hello/gitmir-claude-control.git"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node server.ts",
    "typecheck": "tsc --noEmit"
  },
  "bin": {
    "gitmir": "bin/gitmir.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.8.0"
  },
  "comments": {
    "dependencies": "Empty, and staying that way. The diagram renderer is written for this project and the fonts are vendored under vendor/, so the tool has no supply chain to trust and works offline.",
    "devDependencies": "typescript is for `npm run typecheck` only. Nothing is installed to RUN this \u2014 Node executes the .ts files directly, so `git clone && node server.ts` still needs nothing.",
    "engines": "Node strips TypeScript types natively from 22.18. Node 18 and 20 are both past end of life.",
    "bin": "So `npm i -g .` and `npm i -g github:gitmir-hello/gitmir-claude-control` both give you the same `gitmir` command the curl installer does. Nothing is published to npm; this is here so the two paths cannot diverge."
  }
}
